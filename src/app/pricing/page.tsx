import { PricingView } from 'src/sections/pricing/view';

export const metadata = {
  title: 'Pricing | enableD',
  description: 'Simple, transparent pricing for enableD workplace accommodation management. Choose the plan that fits your organisation.',
};

export default function PricingPage() {
  return <PricingView />;
}
