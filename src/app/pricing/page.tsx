import { PricingView } from 'src/sections/pricing/view';

export const metadata = {
  title: 'Pricing | enableD',
  description: 'Two plans, no long-term contracts, cancel any time. Both include a 14-day free trial. enableD reasonable adjustments platform.',
};

export default function PricingPage() {
  return <PricingView />;
}
