import { AboutView } from 'src/sections/about/view';

export const metadata = {
  title: 'About | enableD',
  description: 'Dave Boland is the founder of enableD. He built it after his own reasonable adjustments were mishandled — and two years in the legal system followed.',
};

export default function AboutPage() {
  return <AboutView />;
}
